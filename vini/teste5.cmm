void mistura(void) {
    int a;
    a = 10;
    
    // Erro sintático: Em C--, variáveis não podem ser declaradas depois de um statement
    int b; 
    b = 20;
    
    while (a > 0) {
        a = a - 1;
        // Erro sintático: O bloco de statement do while não aceita 'Type VarDeclaration'
        char c; 
    }
}